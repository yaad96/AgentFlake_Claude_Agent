ReproFlake-C9E6/data/

# Python bytecode caches — auto-generated, must never be committed
__pycache__/
*.pyc
*.pyo
*.pyd

# macOS Finder metadata — purely Finder UI state, no code value
.DS_Store

# Claude Code session state — local-only, must not be committed
.claude/
